div
  div(plug="content")
    h1(t) title
    p(t) desc
    br
    .m-auto.text-left.text-muted
      p(t) p.0
      p #[span(t) p.1.0] #[a(href="#",onclick="lda.ldcvmgr.toggle('contact')",t) contact] #[span(t) p.1.1]
  div(plug="cta")
    br
    .row
      .col-md.mb-2: .btn.btn-outline-dark.btn-block.btn-lg.mr-2(onclick="lda.ldcvmgr.toggle('contact')",t) contact
      .col-md: a.btn.btn-primary.btn-block.btn-lg.mr-2(ld="reset",t) reset

  script(type="@plotdb/block"): :lsc
    module.exports =
      pkg:
        extend: {ns: "local", name: "error", path: "common.html"}
        i18n:
          en:
            "title": "Login Session Change"
            "desc": "Your login stage information does not match the host, so the action you just performed is temporarily unavailable."
            "p": [
              "It may be because the host information has changed or the login stage has expired, making your login information no longer able to authenticate your identity. You can log in again or try using incognito mode to try again, which should solve the problem."
              [
                "If this problem persists, you can also"
                "see if there is anything we can help with."
              ]
            ]
            "contact": "Contact Us"
            "reset": "Reset Session"
          "zh-TW":
            "title": "登入階段變更"
            "desc": "您的登入階段資訊與主機不符，所以暫時無法進行剛剛的動作"
            "p": [
              "可能是因為主機資訊更動或是登入階段過期，使得您的登入資訊已無法用來認證您的身分。您可以重新登入一次，或者嘗試使用無痕模式嘗試重做，應該就可以解決這個問題了。"
              [
                "若這個問題持續發生，您亦可"
                "看看是否有什麼是我們可以幫上忙的。"
              ]
            ]
            "contact": "聯繫我們"
            "reset": "重設登入階段"

      init: ({root}) ->
        ({core}) <- servebase.corectx _
        view = new ldview do
          root: root
          action: click: reset: -> core.auth.reset!
