//- view
extends /base.pug
block vars
  //-
    - ctrl.navtop.className = "navbar-light bg-semi-light";
    - var meta = { title: "設定" };
block body
  include /modules/navtop.pug
  .d-flex.flex-wrap.mb-0.w-768.mx-auto.rwd
    div(style="flex:1 0 auto;padding-bottom:100px")
      .w-768.rwd(style="padding:30px")
        h1.mt-4.mb-4(pd="t") settings
        br
        .mb-5(ld="account-info")
        br
        div(ld="change-password")

        if false
          +nbr(3)
          h3(pd="t") 危險區域
          hr
          p(pd="t") 要刪除帳號的話，請點擊以下按鈕，並依指示再次輸入您的電子郵件地址做為確認。
          .btn.btn-danger(pd="t",onclick="lda.ldcvmgr.toggle('delete-account')") 刪除帳號

block script
  //+script("/js/me/settings.js")
  +script([
    {name: "ldform"},
    {name: "ldfile"}
  ])
  script(type="module"): include:lsc settings.ls
