.ldcv: .base: .inner.position-relative
  script(type="@plotdb/block"): include:lsc index.ls
  style(type="text/css"): include:stylus index.styl
  video(ld="video",autoplay,playsinline)
  .text-lg(ld="close",data-ldcv-set=""): i.i-close
  .d-none(ld="info")
    .text-sm(ld="isbn")
    .btn-group.mr-1
      .btn.btn-outline-secondary(ld="title")
      .btn.btn-primary(ld="get") 確定
    .btn.btn-primary(ld="manual") 手動
